//Write a code to check whether the number is odd or even
//Define the number
const angkaGanjilGenap : number = 20;

//Check if the number remainder, if 1 then odd, if 0 then even
if(angkaGanjilGenap % 2 == 1) {
    console.log(`${angkaGanjilGenap} adalah bilangan ganjil`);
} else {
    console.log(`${angkaGanjilGenap} adalah bilangan genap`);
}